# Incident Report: Crossposting Feature Outage

**Incident ID**: INC-2026-02-14-001  
**Date Reported**: February 14, 2026  
**Status**: 🔴 ACTIVE - Service Down  
**Severity**: P0 - Critical  
**Feature**: Facebook/Instagram Crossposting  

---

## Summary

Facebook and Instagram crossposting has stopped functioning. Announcements are no longer being posted to social media platforms when they contain multiple images in the `image_urls` array.

---

## Timeline

| Time | Event |
|------|-------|
| Feb 14, 2026 (Prior) | Multi-image detection logic added to `FacebookServiceImpl` |
| Feb 14, 2026 | Code modified to check `imageUrls.size() > 1` and call `postWithMultipleImages()` |
| Feb 14, 2026 | **Method `postWithMultipleImages()` was never implemented** |
| Feb 14, 2026 | User reports: "crossposting feature has stopped functioning as there are no new posts being made" |
| Feb 14, 2026 | Investigation reveals method call to non-existent method |

---

## Impact Assessment

### Affected Users
- **Admin users creating announcements with multiple images**: Cannot crosspost to social media at all
- **Admin users creating announcements with single image**: Potentially still functional
- **Students/end users**: No direct impact (backend issue)

### Affected Functionality
- ❌ **Facebook multi-image posts**: Completely broken
- ❌ **Instagram multi-image carousels**: Completely broken (if routed through same logic)
- ⚠️ **Facebook single-image posts**: May still work (code path exists)
- ⚠️ **Facebook text-only posts**: May still work (code path exists)
- ✅ **Announcement creation**: Still functional (crossposting is separate step)
- ✅ **Announcement viewing**: Unaffected

### Business Impact
- **High**: Social media outreach capability completely lost for multi-image announcements
- **Medium**: Alternative posting methods still available (single image, text-only)
- **Reputation**: Risk if users notice delays or failures in social media presence

---

## Root Cause Analysis

### Technical Details

**File**: `src/main/java/com/soanar/service/impl/FacebookServiceImpl.java`

**Problem Code** (Line 76-79):
```java
List<String> imageUrls = announcement.getImageUrls();
if (imageUrls != null && imageUrls.size() > 1) {
    logger.info("Posting Facebook announcement with {} images", imageUrls.size());
    postId = postWithMultipleImages(pageId, token.get(), postCaption, imageUrls);
}
```

**Issue**: 
- Method `postWithMultipleImages()` is called but **does not exist anywhere in the file**
- The file ends at line 373 with `buildQueryString()` method
- No implementation of `postWithMultipleImages()` found between lines 1-373

**Expected Behavior**:
Method should exist with signature:
```java
private String postWithMultipleImages(String pageId, String token, String message, List<String> imageUrls) throws Exception {
    // Implementation:
    // 1. Upload each image to /{pageId}/photos?published=false
    // 2. Collect photo IDs from each response
    // 3. POST to /{pageId}/feed with attached_media array
    // 4. Return post ID
}
```

### How This Happened

**Change History**:
1. Initial implementation: Single image posting working correctly
2. User feedback: "tested multiple images for crossposting, it only posted 1 image"
3. Code modified: Added detection for `imageUrls.size() > 1`
4. **Critical Error**: Added method call without implementing the method
5. No compilation errors (method signature may have been declared in interface)
6. No runtime testing before deploying change
7. Feature broken in production

**Prevention Measures**:
- ❌ No unit tests for multi-image posting
- ❌ No integration tests catching missing method
- ❌ No code review before commit
- ❌ No manual testing of multi-image scenario

---

## Resolution Strategies

### Option 1: Emergency Rollback (5 minutes)
**Priority**: Immediate restoration of service

**Action**: Comment out multi-image check to restore single-image functionality

```java
// TEMPORARILY DISABLED: postWithMultipleImages not implemented
// List<String> imageUrls = announcement.getImageUrls();
// if (imageUrls != null && imageUrls.size() > 1) {
//     logger.info("Posting Facebook announcement with {} images", imageUrls.size());
//     postId = postWithMultipleImages(pageId, token.get(), postCaption, imageUrls);
// }
// else if (announcement.getImageUrl() != null && !announcement.getImageUrl().isBlank()) {
if (announcement.getImageUrl() != null && !announcement.getImageUrl().isBlank()) {
    logger.info("Posting Facebook announcement with single image URL: {}", announcement.getImageUrl());
    postId = postWithImageUrl(pageId, token.get(), postCaption, announcement.getImageUrl());
}
```

**Result**: 
- ✅ Single-image posts restored
- ✅ Text-only posts restored
- ❌ Multi-image posts still not available (requires full implementation)

**Timeline**: 
1. Edit code (2 min)
2. Rebuild JAR: `mvn package -DskipTests` (2 min)
3. Restart service (1 min)
4. Test single image post (manual verification)

---

### Option 2: Complete Implementation (30-60 minutes)
**Priority**: Full feature restoration

**Action**: Implement `postWithMultipleImages()` method

**Implementation Steps**:

```java
private String postWithMultipleImages(String pageId, String token, String message, List<String> imageUrls) throws Exception {
    try {
        logger.info("Posting Facebook announcement with {} images as album", imageUrls.size());
        
        // Step 1: Upload each image unpublished and collect photo IDs
        List<String> photoIds = new ArrayList<>();
        
        for (int i = 0; i < imageUrls.size(); i++) {
            String imageUrl = imageUrls.get(i);
            logger.info("Uploading image {}/{}: {}", (i+1), imageUrls.size(), imageUrl);
            
            // Download image from URL
            byte[] imageData = restTemplate.getForObject(imageUrl, byte[].class);
            if (imageData == null || imageData.length == 0) {
                logger.warn("Failed to download image from: {}", imageUrl);
                continue;
            }
            
            // Resize image
            byte[] resizedImage = imageService.resizeForFacebook(imageData);
            
            // Upload to Facebook with published=false
            String photoId = uploadUnpublishedPhoto(pageId, token, resizedImage);
            photoIds.add(photoId);
        }
        
        if (photoIds.isEmpty()) {
            throw new RuntimeException("No images were successfully uploaded");
        }
        
        logger.info("Successfully uploaded {} photos, creating feed post with album", photoIds.size());
        
        // Step 2: Create feed post with attached_media
        return createFeedPostWithPhotos(pageId, token, message, photoIds);
        
    } catch (Exception e) {
        logger.error("Failed to post multiple images to Facebook", e);
        throw e;
    }
}

private String uploadUnpublishedPhoto(String pageId, String token, byte[] imageData) throws Exception {
    String photosUrl = String.format("%s/%s/photos", FACEBOOK_API_URL, pageId);
    
    MultiValueMap<String, Object> body = new LinkedMultiValueMap<>();
    body.add("source", new ByteArrayResource(imageData) {
        @Override
        public String getFilename() {
            return "photo.jpg";
        }
    });
    body.add("published", "false");  // KEY: Don't publish yet
    body.add("access_token", token);
    
    HttpHeaders headers = new HttpHeaders();
    headers.setContentType(MediaType.MULTIPART_FORM_DATA);
    HttpEntity<MultiValueMap<String, Object>> request = new HttpEntity<>(body, headers);
    
    ResponseEntity<Map<String, Object>> response = restTemplate.exchange(
        photosUrl,
        HttpMethod.POST,
        request,
        new ParameterizedTypeReference<Map<String, Object>>() {}
    );
    
    if (!response.getStatusCode().is2xxSuccessful()) {
        throw new RuntimeException("Failed to upload unpublished photo");
    }
    
    Map<String, Object> responseBody = response.getBody();
    String photoId = responseBody != null ? (String) responseBody.get("id") : null;
    
    if (photoId == null) {
        throw new RuntimeException("No photo ID returned from Facebook");
    }
    
    logger.info("Uploaded unpublished photo: {}", photoId);
    return photoId;
}

private String createFeedPostWithPhotos(String pageId, String token, String message, List<String> photoIds) throws Exception {
    String feedUrl = String.format("%s/%s/feed", FACEBOOK_API_URL, pageId);
    
    // Build attached_media JSON array
    StringBuilder attachedMedia = new StringBuilder("[");
    for (int i = 0; i < photoIds.size(); i++) {
        if (i > 0) attachedMedia.append(",");
        attachedMedia.append("{\"media_fbid\":\"").append(photoIds.get(i)).append("\"}");
    }
    attachedMedia.append("]");
    
    Map<String, String> params = new LinkedHashMap<>();
    params.put("message", message);
    params.put("attached_media", attachedMedia.toString());
    params.put("access_token", token);
    
    String queryString = buildQueryString(params);
    String url = feedUrl + "?" + queryString;
    
    ResponseEntity<Map<String, Object>> response = restTemplate.exchange(
        url,
        HttpMethod.POST,
        null,
        new ParameterizedTypeReference<Map<String, Object>>() {}
    );
    
    if (!response.getStatusCode().is2xxSuccessful()) {
        throw new RuntimeException("Failed to create feed post with photos");
    }
    
    Map<String, Object> responseBody = response.getBody();
    String postId = responseBody != null ? (String) responseBody.get("id") : null;
    
    if (postId == null) {
        throw new RuntimeException("No post ID returned from Facebook");
    }
    
    logger.info("Created Facebook feed post with {} photos: {}", photoIds.size(), postId);
    return postId;
}
```

**Timeline**:
1. Implement 3 new methods (20 min)
2. Test compilation (2 min)
3. Rebuild JAR: `mvn package -DskipTests` (3 min)
4. Restart service (1 min)
5. Manual testing with multi-image announcement (10 min)
6. Verify post on Facebook (5 min)

**Total**: 40-60 minutes

---

### Option 3: Hybrid Approach (10 minutes + 30-60 minutes)
**Recommendation**: ⭐ **RECOMMENDED**

**Step 1** (Immediate): Deploy Option 1 rollback to restore single-image functionality  
**Step 2** (Follow-up): Implement Option 2 for full multi-image support

**Rationale**: 
- Minimize downtime by quickly restoring partial functionality
- Allows time for proper implementation and testing of multi-image feature
- Reduces risk of introducing new bugs under time pressure

---

## Testing Plan

### Verification Steps (Post-Fix)

1. **Text-only post**:
   - Create announcement without images
   - Enable Facebook crossposting
   - Verify post appears on Facebook with correct text

2. **Single image post**:
   - Create announcement with 1 image
   - Enable Facebook crossposting
   - Verify post appears on Facebook with image and text

3. **Multi-image post** (after full implementation):
   - Create announcement with 3+ images
   - Enable Facebook crossposting
   - Verify album/carousel appears on Facebook with all images

4. **Error handling**:
   - Test with invalid image URL
   - Test with expired credentials
   - Verify graceful error messages

---

## Prevention Measures

### Immediate Actions
- [ ] Add unit tests for `postWithMultipleImages()` method
- [ ] Add integration test for multi-image crossposting flow
- [ ] Add pre-commit hook to verify all called methods exist
- [ ] Document all incomplete/stubbed methods clearly

### Long-term Actions
- [ ] Implement CI/CD pipeline with automated testing
- [ ] Require code review for all service layer changes
- [ ] Add feature flags to enable/disable multi-image support
- [ ] Create staging environment for testing before production
- [ ] Add monitoring/alerting for crossposting failures

---

## Communication

### Internal Stakeholders
- Development team: Notified via this incident report
- QA team: Testing required before closure

### External Stakeholders
- End users: No direct notification needed (backend issue)
- Content admins: May need notification if they've been attempting multi-image posts

---

## Resolution Status

- [ ] Root cause identified
- [ ] Fix implemented
- [ ] Fix tested in development
- [ ] Fix deployed to production
- [ ] Service verified functional
- [ ] Incident documentation complete
- [ ] Post-mortem scheduled

**Assigned To**: Development Team  
**Target Resolution**: Within 1 hour of incident discovery  
**Actual Resolution**: _Pending_

---

## Post-Mortem Notes

_To be completed after incident resolution_

**What went well**:
- 

**What went wrong**:
- Method was called before being implemented
- No compilation errors caught the issue
- No testing caught the issue before user report

**Action items**:
- 

